<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:AvaloniaApplicationTest.ViewModels;assembly=AvaloniaApplicationTest"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        x:Class="AvaloniaApplicationTest.Views.MainWindow"
        Icon="/Assets/avalonia-logo.ico"
        Title="AvaloniaApplicationTest"
		Name="root">

    <Design.DataContext>
        <vm:MainWindowViewModel/>
    </Design.DataContext>

	<DataGrid	x:Name="DataGrid"
				Items="{Binding Students}"
				ScrollViewer.HorizontalScrollBarVisibility="Visible"
				ScrollViewer.VerticalScrollBarVisibility="Visible"
				Height="350"
				Margin="50">
		<DataGrid.Columns>
			<DataGridTextColumn Header="{Binding Path=DataContext.NamesHeader, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
								Binding="{Binding Name, Mode=OneWay}"
								IsReadOnly="True"
								Width="5*" />
			<DataGridTextColumn Header="{Binding Path=DataContext.GradesHeader, ElementName=root}"
								Binding="{Binding Grade, Mode=OneWay}"
								IsReadOnly="True"
								Width="1*" />
			<DataGridTemplateColumn Header="{Binding $parent[Window].DataContext.PresenceHeader}"
									Width="0.5*" >
				<DataGridTemplateColumn.CellTemplate>
					<DataTemplate>
						<CheckBox	IsChecked="{Binding Present, Mode=Default}"/>
					</DataTemplate>
				</DataGridTemplateColumn.CellTemplate>
			</DataGridTemplateColumn>
		</DataGrid.Columns>
	</DataGrid>

</Window>
